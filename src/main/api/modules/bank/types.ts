export type PostBank = {
    name: string;
    userId: number;
}

export type Bank = {
    id: number;
    userId: number;
    name: string;
}

export type BankDTO = {
    id: number;
    user_id: number;
    name: string;
}